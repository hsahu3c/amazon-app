# Amazon Home Module

## Testing

- The variable `state` have the `user_details` collection `user_id` field value. `sAppId` is the `_id` of `app_shops` collection.
 
  Run: https://api.local.cedcommerce.com/apiconnect/request/auth?sAppId=2&state=20

- Authorize on **Amazon** and Check `user_details` collection, the shop will get added.
